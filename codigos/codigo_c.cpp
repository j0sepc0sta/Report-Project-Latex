#include <algorithm>
#include <vector>

int main()
{
    std::vector<int>nums;
    num.push_back(2);
    num.push_back(4);
    num.push_back(3);
    num.push_back(1);
    std::sort(nums.begin(), nums.end())
}
